# 👗 Tôa Tôa Moda Festa - Sistema de Gestão

Este é o sistema de gestão de estoque e catálogo digital da **Tôa Tôa Moda Festa**, patrocinadora oficial do Miss Mato Grosso. O sistema foi projetado para facilitar o controle de vestidos (Noivas, Madrinhas, Formandas e Debutantes).

## 🚀 Arquitetura Técnica

- **Frontend**: PHP 8.1+ com Bootstrap 5.
- **Backend (API)**: Node.js (Express) hospedado no Render (URL: `api-toa-a-toa-2.onrender.com`).
- **Banco de Dados**: PostgreSQL via Supabase (Cloud) e MySQL (Local para PDV).
- **Autenticação**: Segurança baseada em `x-api-key`.

## 🎨 Identidade Visual (Paleta de Cores)

- **Azul Profundo**: `#001D3D` (Fundo/Sofisticação)
- **Amarelo Ouro**: `#FFD700` (Bordas e Destaques)
- **Verde Esmeralda**: `#2D8A4E` (Identidade Regional)
- **Vermelho Vivo**: `#E60000` (Alertas)

## 🛠️ Estrutura do Projeto

- `index.php`: Formulário principal para cadastro de produtos.
- `produtos.php`: Listagem de estoque, filtros e gerador de link de catálogo.
- `services/toa_toa_api.php`: Cliente HTTP centralizado para a API TOA-TOA.
- `db.php`: Funções de compatibilidade usadas pelas telas PHP.
- `editar_produto.php`: Edição de dados e exclusão lógica/física de itens.
- `cadastro_cliente.php` / `clientes_cadastrados.php`: Módulo completo de CRM.
- `venda.php`: Interface de fechamento de negócios (vendas e aluguéis).
- `finalizar_venda.php`: Processamento de encerramento de venda e baixa de estoque.
- `navbar.php`: Cabeçalho reutilizável com sistema de busca rápida.
- `index.css`: Definições globais de estilo e cores.
- `atualizador.php`: Script de sincronização automática com o GitHub.

## 📸 Regras de Imagem
- O arquivo JPEG, PNG ou WebP é enviado como `multipart/form-data` para a API.
- O projeto não acessa o Supabase Storage diretamente.
- A URL devolvida pela API é renderizada sem reconstrução manual.
- Imagens ausentes ou inválidas utilizam `toatoa.png`.

## 📋 Requisitos de Banco de Dados (SQL)
### Supabase (PostgreSQL)
```sql
CREATE TABLE produtos (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome TEXT NOT NULL,
    categoria TEXT,
    validade DATE,
    quantidade INTEGER DEFAULT 0,
    preco_unitario NUMERIC(10, 2) DEFAULT 0.00,
    preco_pacote NUMERIC(10, 2) DEFAULT 0.00,
    imagem TEXT,
    descricao TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE clientes (
    id SERIAL PRIMARY KEY,
    nome_completo TEXT NOT NULL,
    cpf VARCHAR(14) UNIQUE NOT NULL,
    rg VARCHAR(20),
    whatsapp VARCHAR(20) NOT NULL,
    tipo_contato_1 VARCHAR(30) DEFAULT 'Pessoal',
    telefone_secundario VARCHAR(20),
    tipo_contato_2 VARCHAR(30),
    email TEXT,
    cep VARCHAR(9),
    endereco TEXT,
    preferencias TEXT,
    data_evento DATE,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Ver reademe.md para as tabelas de Vendas e Itens_Venda
```

## ⚙️ Configuração e Instalação

1. Habilite as extensões `curl`, `fileinfo` e `zip` no `php.ini`.
2. Crie um `.env` local baseado em `.env.example`.
3. Configure somente no backend:
   ```env
   API_BASE_URL=https://api-toa-a-toa-2.onrender.com
   TOA_TOA_API_KEY=valor_da_CHAVE_MESTRA_configurada_no_Render
   ```
4. Nunca envie `TOA_TOA_API_KEY` ao navegador ou ao Git.
5. Execute localmente:
    ```bash
    php -S localhost:8000
    ```
6. Execute os testes isolados:
   ```bash
   php tests/run.php
   ```

O caminho do arquivo de configuração pode ser confirmado com `php --ini`.

## 📌 Funcionalidades Principais

- [x] Cadastro de produtos com upload de imagem.
- [x] Preview de imagem em tempo real antes do salvamento.
- [x] Listagem de estoque com suporte a ID automático.
- [x] Diferenciação visual entre preços de venda e aluguel.
- [x] **Busca Automática:** Filtro instantâneo na tabela enquanto o usuário digita.
- [x] **Busca Precisa:** Consulta completa à API ao pressionar Enter, mantendo o foco no campo.

## 🔄 Como Atualizar o Sistema
O sistema verifica atualizações automaticamente ao abrir uma tela e quando o app volta ao primeiro plano. A interface consulta `version.json` sem bloquear o uso e instala somente uma versão superior.

### Publicar uma versão

1. Finalize e teste as alterações.
2. Gere um pacote imutável, aumentando a versão:
   ```powershell
   powershell -ExecutionPolicy Bypass -File tools/build-update-package.ps1 -Version 1.1.0
   ```
3. Revise o `version.json`. O script preenche `download_url`, `sha256`, `minimum_app_version` e `published_at`.
4. Inclua o pacote mesmo estando ignorado por padrão:
   ```bash
   git add -A
   git add -f releases/toa-toa-1.1.0.zip
   git commit -m "Publica versão 1.1.0"
   git push origin main
   ```
5. As instalações consultarão o manifesto em até 5 minutos e aplicarão a atualização. A verificação manual continua disponível em `atualizador.php`.

Nunca aponte `download_url` para o ZIP dinâmico da branch: o pacote e seu SHA-256 precisam ser imutáveis.

### Segurança e recuperação

- Somente URLs HTTPS do GitHub são aceitas.
- O SHA-256 é validado antes da extração.
- Caminhos absolutos, `..` e links simbólicos são rejeitados.
- `.env`, credenciais, bancos locais, `settings.json`, `imagens/` e dados do usuário não são substituídos.
- Os arquivos são preparados temporariamente e trocados individualmente.
- Antes de cada troca há um backup; uma falha restaura automaticamente a versão anterior.
- Estado, logs e backups ficam em `storage/update/` e não são enviados ao Git.
- Sem internet, a versão instalada continua funcionando e a verificação é repetida depois.

### PWA, WebView e lojas

Este mecanismo atualiza a aplicação PHP hospedada no servidor. Uma PWA ou WebView passa a exibir as novas telas após recarregar. Ele não substitui código nativo de Android ou iOS: mudanças no binário, permissões, plugins nativos ou configuração da loja exigem uma nova versão na Play Store ou App Store.

### Testar o atualizador

```bash
php tests/update_service_test.php
php tests/run.php
```

Os testes do atualizador cobrem versão nova, versão atual, indisponibilidade de rede, pacote corrompido, SHA inválido, URL não autorizada, preservação de dados e rollback.

## 📲 Catálogo público e WhatsApp

O catálogo compartilhado não depende da sessão administrativa. Os IDs selecionados ficam na URL pública e os dados são consultados no servidor, sem expor a chave da API ao navegador.

Configure no `.env` do servidor:

```env
PUBLIC_APP_URL=https://catalogo.seudominio.com
WHATSAPP_BUSINESS_NUMBER=5565999999999
```

`PUBLIC_APP_URL` precisa ser o endereço HTTPS público desta aplicação PHP. Um endereço `localhost`, IP de rede interna ou pasta existente somente no computador da loja não pode ser aberto pelo cliente.

Na tela de produtos:

1. Use **Criar Catálogo** para incluir todos os produtos visíveis, ou marque apenas os desejados.
2. Clique em **Gerar Link do Catálogo**.
3. Use **Enviar pelo WhatsApp**, o compartilhamento nativo do celular ou copie o link.

A página `catalogo.php` é pública, mas a chave da API permanece somente no servidor. Se a API ficar temporariamente indisponível, o servidor exibe o último catálogo armazenado em `storage/catalog/products.json`.

Teste o fluxo com:

```bash
php tests/catalog_test.php
```

---

## 📝 Planejamento (Roadmap)

### Curto Prazo
- [ ] Formalização do cadastro de clientes.
- [x] Visualização de ficha completa do cliente.

### Médio Prazo
- [ ] Histórico detalhado de aluguéis.
- [ ] Automação total de fluxo de caixa.
- [ ] Geração de catálogo direto para WhatsApp.

### Longo Prazo
- [ ] Transformação do sistema em um E-commerce de vendas e reservas online.

---
📂 Comandos Úteis (Git/Deploy)
1. `git add .`
2. `git commit -m "Atualização de funcionalidades e busca"`
3. `git push origin main`

git add -A
git commit -m "Atualiza integração e conexão com a API"
git push -u origin main

**Tôa Tôa Moda Festa**
*Patrocinadora oficial do Miss Mato Grosso*
